export type QuestionProps = {
  pista: string;
  resposta: string;
};

export type LetterProps = {
  letter: string;
  visibility: boolean;
};

export type AlfabetoProps = {
  letter: string;
  disabled: boolean;
};
